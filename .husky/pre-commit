#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run lint-staged (includes linting, formatting, and tests on staged files)
npx lint-staged

echo "✅ Pre-commit checks passed!"
